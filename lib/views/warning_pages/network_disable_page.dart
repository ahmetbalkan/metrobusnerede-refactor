import 'package:flutter/material.dart';

class NetworkDisablePage extends StatelessWidget {
  const NetworkDisablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Network Disable")),
    );
  }
}
